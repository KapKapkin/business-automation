services:
  nginx:
    image: nginx:1.29-alpine
    container_name: ba-nginx-local
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - flask
    networks:
      - ba-network
